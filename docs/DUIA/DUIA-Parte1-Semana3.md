# DUIA - Parte 1: Generación Masiva de Datos de Prueba (Semana 3)

| **Herramienta** | Generación de script SQL transaccional PostgreSQL (OpenCode) |
| **Spec o prompt utilizado** | "Generar script SQL transaccional para PostgreSQL que inserte datos masivos de prueba usando generate_series: Tabla producto 50K filas, precio aleatorio 500-5000, stock 0-200, categoria_id aleatorio existente; Tabla usuario 20K filas, correos únicos (userN@test.com), contraseña genérica; Tabla pedido 200K filas, estados y formas de pago aleatorios basados en ENUMs, usuarios aleatorios; Tabla detalle_pedido ≥1 detalle por pedido, manejando conflictos de PK compuesta; Restricciones: Sin funciones PL/pgSQL, todo dentro de BEGIN/COMMIT, seguido de ANALYZE en las 4 tablas" |
| **Qué generó** | Script base usando generate_series para todas las inserciones masivas, RANDOM() para valores aleatorios (precio, stock, IDs), CASE expressions para valores ENUM (estado_pedido, forma_pago), subquery SELECT id FROM categoria ORDER BY random() LIMIT 1 para categoria_id aleatorio, DISTINCT ON + ON CONFLICT DO NOTHING para detalle_pedido, ANALYZE final en las 4 tablas |
| **Qué se aceptó** | La estructura general del script transaccional, el uso de generate_series, el enfoque de inserción masiva, la lógica de ANALYZE final, la organización en 4 bloques INSERT |
| **Qué se modificó o descartó, y por qué** | categoria_id: cambió FLOOR(RANDOM() * COUNT(*) + 1) por (SELECT id FROM categoria ORDER BY random() LIMIT 1) — selección real aleatoria vs cálculo sobre COUNT (evita gaps si hay IDs eliminados); precio: cambió FLOOR(RANDOM() * 4501 + 500)::INTEGER por (random() * 4500 + 500)::numeric(10,2) — precio como NUMERIC(10,2) con decimales, no entero; celular: cambió '11' || RIGHT('000' || (i % 9999), 4) por '261' || lpad((random()*9999999)::int::text, 7, '0') — formato argentino (261 = Mendoza), 10 dígitos, aleatorio real; fecha pedido: agregado CURRENT_DATE - (random()*365)::int — fechas realistas en último año; ENUM casting: cambió CASE FLOOR(RANDOM() * N) por (ARRAY[...]::tipo[])[floor(random()*N+1)] — casting directo a ENUM, más limpio y seguro; detalle_pedido: cambió 1 fila por pedido (LIMIT 200000) por 1-4 líneas por pedido con row_number() + CROSS JOIN LATERAL — distribución realista, evita duplicados con ON CONFLICT DO NOTHING; ANALYZE: 4 líneas separadas → una línea con ; — compacto, equivalente |
| **Verificación realizada** | Prueba de equivalencia con EXCEPT bidireccional comparando conteos esperados vs reales. Ejecución en PostgreSQL: producto 50.000 ✅, usuario 20.000 ✅, pedido 200.000 ✅, detalle_pedido ~500.000 ✅ (variable 1-4 líneas/pedido). Triggers verificados: trg_01_validar_producto_activo no bloqueó ✅, trg_02_validar_stock no bloqueó ✅, trg_03_calcular_subtotal ejecutó correctamente completando precio_unitario y subtotal ✅. ANALYZE ejecutado: estadísticas actualizadas para planner ✅. |